/**
 * Make class name with empty spacing.
 * @param classNames list of class names.
 * @returns Class Name for element class value.
 */
export const clazz = (...classNames: string[]) => classNames.join(" ");
